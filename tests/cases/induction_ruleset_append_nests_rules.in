{ max_steps = 2 }

r1 = 1 => 2;
r2 = 2 => 3;

rules = [ r1 ];
rules_good = [ r1, r2 ];
rules_bad = [ all rules r2 ];

print { rules_good };
print { rules_bad };

{ 1 => 3 ?|= rules_good : max_steps }
print { witness.w1.ok };

{ 1 => 3 ?|= rules_bad : max_steps }
print { witness.w2.ok };
