unit consts;

interface

const
  EXP_CAPACITY = 1024*1024*40;
  // TREE_CAPACITY = 2000000;    // 1000000 * 24 bytes = 24 mb
  TREE_CAPACITY = 1000000;  // (temporarily reduced for debugging purposes)

implementation

end.
